/** Action Order / CELO-cards vs-house skill id. */
export const ACTIONORDER_SKILL_ID =
  "gaming/card-fighter/actionorder_vshouse" as const;

/** Production CELO-cards API base (Action Order Season 2). */
export const ACTIONORDER_DEFAULT_URL = "https://www.actionorder.xyz";

/**
 * Minimum actionorder-player package version with secure start/resolve flow
 * (difficulty pinned at start, x-agent-key, no difficulty on resolve).
 */
export const ACTIONORDER_MIN_SECURE_SKILL_VERSION = "1.1.0";
